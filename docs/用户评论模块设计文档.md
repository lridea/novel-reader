# 用户评论模块设计文档

**文档版本**: v1.0
**设计日期**: 2026-02-18
**设计人员**: OpenClaw
**状态**: 待评审

---

## 📋 需求概述

### 功能需求

| 序号 | 功能 | 描述 | 优先级 | 状态 |
|------|------|------|--------|------|
| 1 | 添加评论 | 登录后的用户可以在书籍详情页评论 | P0 | 待开发 |
| 2 | 回复评论 | 最上层的评论可以被回复（支持楼层结构） | P0 | 待开发 |
| 3 | 展示评论数 | 在书籍列表展示评论数 | P0 | 待开发 |
| 4 | 评论列表 | 在书籍详情页展示评论列表（参考b站） | P0 | 待开发 |
| 5 | 点赞评论 | 用户可以点赞/取消点赞评论（可选） | P1 | 待开发 |
| 6 | 删除评论 | 用户可以删除自己的评论（可选） | P1 | 待开发 |

---

## 🗄️ 数据库设计

### 1. 修改 t_novel 表

**新增字段**：

```sql
-- 添加评论数字段
ALTER TABLE t_novel ADD COLUMN comment_count INT DEFAULT 0 COMMENT '评论数' AFTER favorite_count;

-- 添加评论数字段索引
CREATE INDEX idx_comment_count ON t_novel(comment_count);
```

**字段说明**：
- `comment_count`: 评论数，默认0

---

### 2. 新建 t_comment 表（评论表）

```sql
-- 评论表
CREATE TABLE IF NOT EXISTS t_comment (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    novel_id BIGINT NOT NULL COMMENT '小说ID',
    parent_id BIGINT COMMENT '父评论ID（NULL表示顶层评论）',
    floor INT DEFAULT 1 COMMENT '楼层：1-顶层评论，2-回复评论',
    content TEXT NOT NULL COMMENT '评论内容',
    like_count INT DEFAULT 0 COMMENT '点赞数',
    reply_count INT DEFAULT 0 COMMENT '回复数（仅顶层评论）',
    deleted TINYINT DEFAULT 0 COMMENT '是否删除: 0-否, 1-是',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    
    INDEX idx_novel_id (novel_id),
    INDEX idx_parent_id (parent_id),
    INDEX idx_user_id (user_id),
    INDEX idx_created_at (created_at DESC),
    INDEX idx_novel_floor (novel_id, floor, created_at DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='评论表';
```

**字段说明**：
- `id`: 主键ID
- `user_id`: 评论用户ID
- `novel_id`: 小说ID
- `parent_id`: 父评论ID，NULL表示顶层评论
- `floor`: 楼层，1-顶层评论，2-回复评论
- `content`: 评论内容
- `like_count`: 点赞数
- `reply_count`: 回复数（仅顶层评论统计）
- `deleted`: 是否删除
- `created_at`: 创建时间
- `updated_at`: 更新时间

**索引说明**：
- `idx_novel_id`: 小说ID索引（查询小说评论）
- `idx_parent_id`: 父评论ID索引（查询回复）
- `idx_user_id`: 用户ID索引（查询用户评论）
- `idx_created_at`: 创建时间索引（按时间排序）
- `idx_novel_floor`: 小说+楼层+时间索引（优化顶层评论和回复查询）

---

## 🌐 API接口设计

### 1. 获取评论列表（分页）

**接口路径**: `GET /api/comments/novel/{novelId}`

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| novelId | Long | 是 | 小说ID |

**查询参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| page | Integer | 否 | 0 | 页码（从0开始） |
| size | Integer | 否 | 10 | 每页数量 |
| floor | Integer | 否 | 1 | 楼层（1-顶层评论，2-回复评论） |
| parentId | Long | 否 | null | 父评论ID（查询回复时使用） |

**请求示例**：

示例1：获取顶层评论列表
```http
GET /api/comments/novel/1?page=0&size=10&floor=1
```

示例2：获取回复列表
```http
GET /api/comments/novel/1?page=0&size=10&floor=2&parentId=100
```

**响应格式**：

```json
{
  "success": true,
  "content": [
    {
      "id": 100,
      "userId": 1,
      "user": {
        "id": 1,
        "username": "testuser",
        "nickname": "测试用户",
        "avatar": "https://example.com/avatar.jpg"
      },
      "novelId": 1,
      "parentId": null,
      "floor": 1,
      "content": "这本书太好看了！",
      "likeCount": 10,
      "replyCount": 5,
      "liked": false,
      "isOwner": false,
      "createdAt": "2026-02-18T10:00:00",
      "updatedAt": "2026-02-18T10:00:00"
    }
  ],
  "totalElements": 50,
  "totalPages": 5,
  "size": 10,
  "number": 0
}
```

**字段说明**：
- `user`: 用户信息（包含username、nickname、avatar）
- `liked`: 当前用户是否已点赞（登录后返回，未登录返回null）
- `isOwner`: 当前用户是否为评论作者（登录后返回，未登录返回false）

---

### 2. 添加评论

**接口路径**: `POST /api/comments`

**请求体**：

```json
{
  "novelId": 1,
  "parentId": null,
  "floor": 1,
  "content": "这本书太好看了！"
}
```

**字段说明**：
- `novelId`: 小说ID（必填）
- `parentId`: 父评论ID（回复时必填，顶层评论为null）
- `floor`: 楼层（1-顶层评论，2-回复评论）
- `content`: 评论内容（必填）

**响应格式**：

```json
{
  "success": true,
  "message": "评论成功",
  "comment": {
    "id": 100,
    "userId": 1,
    "novelId": 1,
    "parentId": null,
    "floor": 1,
    "content": "这本书太好看了！",
    "likeCount": 0,
    "replyCount": 0,
    "liked": false,
    "isOwner": true,
    "createdAt": "2026-02-18T10:00:00"
  }
}
```

**后端逻辑**：
1. 检查用户是否登录
2. 检查小说是否存在
3. 如果是回复，检查父评论是否存在
4. 创建评论记录
5. 更新小说的评论数（+1）
6. 如果是回复，更新父评论的回复数（+1）
7. 返回评论信息

---

### 3. 删除评论

**接口路径**: `DELETE /api/comments/{id}`

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 评论ID |

**响应格式**：

```json
{
  "success": true,
  "message": "删除成功",
  "novelCommentCount": 49
}
```

**后端逻辑**：
1. 检查用户是否登录
2. 检查评论是否存在
3. 检查当前用户是否为评论作者
4. 标记删除（deleted=1），不物理删除
5. 更新小说的评论数（-1）
6. 如果是回复，更新父评论的回复数（-1）
7. 返回最新的评论数

---

### 4. 点赞评论

**接口路径**: `POST /api/comments/{id}/like`

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 评论ID |

**响应格式**：

```json
{
  "success": true,
  "message": "点赞成功",
  "commentLikeCount": 11,
  "liked": true
}
```

**后端逻辑**：
1. 检查用户是否登录
2. 检查评论是否存在
3. 检查是否已点赞（需要评论点赞表）
4. 如果未点赞，添加点赞记录，点赞数+1
5. 如果已点赞，取消点赞记录，点赞数-1
6. 返回最新的点赞数和点赞状态

**注意**：此功能需要新建评论点赞表（t_comment_like）

---

### 5. 取消点赞评论

**接口路径**: `DELETE /api/comments/{id}/like`

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 评论ID |

**响应格式**：

```json
{
  "success": true,
  "message": "取消点赞成功",
  "commentLikeCount": 10,
  "liked": false
}
```

---

## 🎨 前端界面设计

### 1. 小说详情页面（NovelDetail.vue）

#### 1.1 评论数展示

在小说信息中新增评论数展示：

```vue
<div class="meta-info">
  <el-tag>{{ getPlatformName(novel.platform) }}</el-tag>
  <span class="author">作者：{{ novel.author || '未知' }}</span>
  <el-tag :type="getStatusType(novel.status)">
    {{ getStatusText(novel.status) }}
  </el-tag>
  <span class="favorite-count">
    <el-icon><Star /></el-icon>
    {{ novel.favoriteCount || 0 }}
  </span>
  <span class="comment-count">
    <el-icon><ChatDotRound /></el-icon>
    {{ novel.commentCount || 0 }}
  </span>
</div>
```

**展示效果**：
- 评论数用蓝色显示，带评论图标
- 未登录时也显示，所有用户可见

---

#### 1.2 评论区域

在AI概括卡片下方新增评论区域：

```vue
<el-card shadow="hover" style="margin-top: 20px;" v-if="novel.firstChaptersSummary">
  <template #header>
    <span>评论 ({{ novel.commentCount || 0 }})</span>
  </template>
  <div class="ai-summary">
    {{ novel.firstChaptersSummary }}
  </div>
</el-card>

<!-- 评论区域 -->
<el-card shadow="hover" style="margin-top: 20px;">
  <template #header>
    <div class="comment-header">
      <span>评论 ({{ totalComments }})</span>
      <el-button type="primary" size="small" @click="showAddCommentDialog">
        发表评论
      </el-button>
    </div>
  </template>

  <!-- 顶层评论列表 -->
  <div class="comment-list" v-loading="commentsLoading">
    <div class="comment-item" v-for="comment in topLevelComments" :key="comment.id">
      <!-- 评论内容 -->
      <div class="comment-main">
        <el-avatar :src="comment.user.avatar" :size="40" />
        <div class="comment-content">
          <div class="comment-header">
            <span class="username">{{ comment.user.nickname || comment.user.username }}</span>
            <span class="time">{{ formatTime(comment.createdAt) }}</span>
          </div>
          <div class="comment-text">{{ comment.content }}</div>
          <div class="comment-actions">
            <el-button
              :type="comment.liked ? 'primary' : 'default'"
              :icon="comment.liked ? StarFilled : Star"
              size="small"
              @click="toggleLike(comment)"
            >
              {{ comment.likeCount }}
            </el-button>
            <el-button
              size="small"
              @click="showReplyDialog(comment)"
            >
              回复
            </el-button>
            <el-button
              v-if="comment.isOwner"
              type="danger"
              size="small"
              @click="deleteComment(comment)"
            >
              删除
            </el-button>
          </div>
        </div>
      </div>

      <!-- 回复列表 -->
      <div class="reply-list" v-if="comment.replyCount > 0">
        <div class="reply-item" v-for="reply in comment.replies" :key="reply.id">
          <el-avatar :src="reply.user.avatar" :size="32" />
          <div class="reply-content">
            <div class="reply-header">
              <span class="username">{{ reply.user.nickname || reply.user.username }}</span>
              <span class="reply-to">回复</span>
              <span class="target-user">@{{ reply.parentUser.nickname || reply.parentUser.username }}</span>
              <span class="time">{{ formatTime(reply.createdAt) }}</span>
            </div>
            <div class="reply-text">{{ reply.content }}</div>
            <div class="reply-actions">
              <el-button
                :type="reply.liked ? 'primary' : 'default'"
                :icon="reply.liked ? StarFilled : Star"
                size="small"
                @click="toggleLike(reply)"
              >
                {{ reply.likeCount }}
              </el-button>
              <el-button
                size="small"
                @click="showReplyDialog(comment, reply)"
              >
                回复
              </el-button>
              <el-button
                v-if="reply.isOwner"
                type="danger"
                size="small"
                @click="deleteComment(reply)"
              >
                删除
              </el-button>
            </div>
          </div>
        </div>

        <!-- 展开更多回复 -->
        <el-button
          v-if="comment.replyCount > comment.replies.length"
          type="primary"
          link
          @click="loadMoreReplies(comment)"
        >
          展开更多回复 ({{ comment.replyCount - comment.replies.length }})
        </el-button>
      </div>
    </div>

    <!-- 空状态 -->
    <el-empty v-if="topLevelComments.length === 0 && !commentsLoading" description="暂无评论" />

    <!-- 分页 -->
    <el-pagination
      v-model:current-page="commentPage"
      v-model:page-size="commentPageSize"
      :total="totalComments"
      :page-sizes="[10, 20, 50]"
      layout="total, sizes, prev, pager, next, jumper"
      @size-change="loadComments"
      @current-change="loadComments"
      style="margin-top: 20px; justify-content: flex-end;"
    />
  </div>
</el-card>
```

**展示效果**（参考b站）：
- 顶层评论展示：用户头像、昵称、时间、内容、点赞数、回复数
- 回复列表展示：用户头像、昵称、回复对象、时间、内容、点赞数
- 回复默认折叠，点击"展开更多回复"加载
- 支持"回复"按钮回复顶层评论
- 支持"点赞"按钮点赞/取消点赞
- 评论作者可以"删除"自己的评论

---

#### 1.3 添加评论对话框

```vue
<el-dialog
  v-model="addCommentDialogVisible"
  title="发表评论"
  width="500px"
  @close="resetAddCommentForm"
>
  <el-form :model="addCommentForm" label-width="80px">
    <el-form-item label="评论内容">
      <el-input
        v-model="addCommentForm.content"
        type="textarea"
        :rows="4"
        placeholder="请输入评论内容..."
        maxlength="500"
        show-word-limit
      />
    </el-form-item>
  </el-form>
  <template #footer>
    <el-button @click="addCommentDialogVisible = false">取消</el-button>
    <el-button type="primary" @click="submitComment">发表</el-button>
  </template>
</el-dialog>
```

---

#### 1.4 回复评论对话框

```vue
<el-dialog
  v-model="replyDialogVisible"
  :title="`回复 ${replyTarget?.user?.nickname || replyTarget?.user?.username}`"
  width="500px"
  @close="resetReplyForm"
>
  <el-form :model="replyForm" label-width="80px">
    <el-form-item label="回复内容">
      <el-input
        v-model="replyForm.content"
        type="textarea"
        :rows="4"
        placeholder="请输入回复内容..."
        maxlength="500"
        show-word-limit
      />
    </el-form-item>
  </el-form>
  <template #footer>
    <el-button @click="replyDialogVisible = false">取消</el-button>
    <el-button type="primary" @click="submitReply">发表</el-button>
  </template>
</el-dialog>
```

---

### 2. 小说列表页面（Novels.vue）

#### 2.1 评论数展示

在表格中新增"评论数"列：

```vue
<el-table-column prop="commentCount" label="评论数" width="100">
  <template #default="{ row }">
    <span style="color: #409EFF; font-weight: 500;">
      <el-icon><ChatDotRound /></el-icon>
      {{ row.commentCount || 0 }}
    </span>
  </template>
</el-table-column>
```

**展示效果**：
- 评论数用蓝色（#409EFF）显示，带评论图标
- 未登录时也显示，所有用户可见

---

## 🔧 后端实现细节

### 1. Comment实体

```java
@Data
@Entity
@Table(name = "t_comment", indexes = {
    @Index(name = "idx_novel_id", columnList = "novelId"),
    @Index(name = "idx_parent_id", columnList = "parentId"),
    @Index(name = "idx_user_id", columnList = "userId"),
    @Index(name = "idx_created_at", columnList = "createdAt"),
    @Index(name = "idx_novel_floor", columnList = "novelId, floor, createdAt DESC")
})
public class Comment {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    /**
     * 用户ID
     */
    @Column(nullable = false)
    private Long userId;

    /**
     * 小说ID
     */
    @Column(nullable = false)
    private Long novelId;

    /**
     * 父评论ID（NULL表示顶层评论）
     */
    private Long parentId;

    /**
     * 楼层：1-顶层评论，2-回复评论
     */
    @ColumnDefault("1")
    private Integer floor = 1;

    /**
     * 评论内容
     */
    @Column(columnDefinition = "TEXT", nullable = false)
    private String content;

    /**
     * 点赞数
     */
    @ColumnDefault("0")
    private Integer likeCount = 0;

    /**
     * 回复数（仅顶层评论统计）
     */
    @ColumnDefault("0")
    private Integer replyCount = 0;

    /**
     * 是否删除
     */
    @ColumnDefault("0")
    private Integer deleted = 0;

    /**
     * 创建时间
     */
    @CreationTimestamp
    @Column(nullable = false, updatable = false)
    private LocalDateTime createdAt;

    /**
     * 更新时间
     */
    @UpdateTimestamp
    @Column(nullable = false)
    private LocalDateTime updatedAt;

    /**
     * 用户信息（DTO字段，不存储到数据库）
     */
    @Transient
    private UserInfo user;

    /**
     * 父评论用户信息（DTO字段，仅回复使用）
     */
    @Transient
    private UserInfo parentUser;

    /**
     * 回复列表（DTO字段，仅顶层评论使用）
     */
    @Transient
    private List<Comment> replies;

    /**
     * 当前用户是否已点赞（DTO字段）
     */
    @Transient
    private Boolean liked;

    /**
     * 当前用户是否为评论作者（DTO字段）
     */
    @Transient
    private Boolean isOwner;
}
```

---

### 2. Novel实体更新

```java
/**
 * 评论数
 */
@ColumnDefault("0")
private Integer commentCount = 0;
```

---

### 3. CommentRepository

```java
@Repository
public interface CommentRepository extends JpaRepository<Comment, Long> {

    /**
     * 根据小说ID和楼层分页查询评论
     */
    Page<Comment> findByNovelIdAndFloorAndDeletedOrderByCreatedAtDesc(
        Long novelId,
        Integer floor,
        Integer deleted,
        Pageable pageable
    );

    /**
     * 根据父评论ID分页查询回复
     */
    Page<Comment> findByParentIdAndDeletedOrderByCreatedAtAsc(
        Long parentId,
        Integer deleted,
        Pageable pageable
    );

    /**
     * 统计小说的顶层评论数
     */
    @Query("SELECT COUNT(c) FROM Comment c WHERE c.novelId = :novelId AND c.floor = 1 AND c.deleted = 0")
    Long countTopLevelCommentsByNovelId(@Param("novelId") Long novelId);

    /**
     * 统计父评论的回复数
     */
    @Query("SELECT COUNT(c) FROM Comment c WHERE c.parentId = :parentId AND c.deleted = 0")
    Long countRepliesByParentId(@Param("parentId") Long parentId);
}
```

---

### 4. CommentService

```java
@Service
public class CommentService {

    @Autowired
    private CommentRepository commentRepository;

    @Autowired
    private NovelRepository novelRepository;

    @Autowired
    private UserRepository userRepository;

    /**
     * 添加评论
     */
    @Transactional
    public Map<String, Object> addComment(Long userId, Long novelId, Long parentId, Integer floor, String content) {
        // 检查小说是否存在
        Optional<Novel> novelOpt = novelRepository.findById(novelId);
        if (novelOpt.isEmpty()) {
            return Map.of("success", false, "message", "小说不存在");
        }

        Novel novel = novelOpt.get();

        // 如果是回复，检查父评论是否存在
        Comment parentComment = null;
        if (floor == 2 && parentId != null) {
            Optional<Comment> parentOpt = commentRepository.findById(parentId);
            if (parentOpt.isEmpty()) {
                return Map.of("success", false, "message", "父评论不存在");
            }
            parentComment = parentOpt.get();
        }

        // 创建评论
        Comment comment = new Comment();
        comment.setUserId(userId);
        comment.setNovelId(novelId);
        comment.setParentId(parentId);
        comment.setFloor(floor);
        comment.setContent(content);
        comment = commentRepository.save(comment);

        // 更新小说的评论数（原子操作）
        novelRepository.incrementCommentCount(novelId);

        // 如果是回复，更新父评论的回复数（原子操作）
        if (floor == 2 && parentComment != null) {
            commentRepository.incrementReplyCount(parentId);
        }

        // 获取最新的小说评论数
        Novel updatedNovel = novelRepository.findById(novelId).orElse(null);

        return Map.of(
            "success", true,
            "message", "评论成功",
            "comment", toCommentInfo(comment),
            "novelCommentCount", updatedNovel != null ? updatedNovel.getCommentCount() : 0
        );
    }

    /**
     * 删除评论
     */
    @Transactional
    public Map<String, Object> deleteComment(Long userId, Long commentId) {
        // 检查评论是否存在
        Optional<Comment> commentOpt = commentRepository.findById(commentId);
        if (commentOpt.isEmpty()) {
            return Map.of("success", false, "message", "评论不存在");
        }

        Comment comment = commentOpt.get();

        // 检查当前用户是否为评论作者
        if (!comment.getUserId().equals(userId)) {
            return Map.of("success", false, "message", "无权删除");
        }

        // 标记删除
        comment.setDeleted(1);
        commentRepository.save(comment);

        // 更新小说的评论数（原子操作）
        novelRepository.decrementCommentCount(comment.getNovelId());

        // 如果是回复，更新父评论的回复数（原子操作）
        if (comment.getFloor() == 2 && comment.getParentId() != null) {
            commentRepository.decrementReplyCount(comment.getParentId());
        }

        // 获取最新的小说评论数
        Novel novel = novelRepository.findById(comment.getNovelId()).orElse(null);

        return Map.of(
            "success", true,
            "message", "删除成功",
            "novelCommentCount", novel != null ? novel.getCommentCount() : 0
        );
    }

    /**
     * 转换为评论信息
     */
    private Map<String, Object> toCommentInfo(Comment comment) {
        Map<String, Object> commentInfo = new HashMap<>();
        commentInfo.put("id", comment.getId());
        commentInfo.put("userId", comment.getUserId());
        commentInfo.put("novelId", comment.getNovelId());
        commentInfo.put("parentId", comment.getParentId());
        commentInfo.put("floor", comment.getFloor());
        commentInfo.put("content", comment.getContent());
        commentInfo.put("likeCount", comment.getLikeCount());
        commentInfo.put("replyCount", comment.getReplyCount());
        commentInfo.put("createdAt", comment.getCreatedAt());

        return commentInfo;
    }
}
```

---

### 5. NovelRepository新增方法

```java
/**
 * 原子操作：评论数+1
 */
@Modifying
@Query("UPDATE Novel n SET n.commentCount = n.commentCount + 1 WHERE n.id = :novelId")
@Transactional
void incrementCommentCount(@Param("novelId") Long novelId);

/**
 * 原子操作：评论数-1
 */
@Modifying
@Query("UPDATE Novel n SET n.commentCount = n.commentCount - 1 WHERE n.id = :novelId AND n.commentCount > 0")
@Transactional
void decrementCommentCount(@Param("novelId") Long novelId);
```

---

### 6. CommentRepository新增方法

```java
/**
 * 原子操作：回复数+1
 */
@Modifying
@Query("UPDATE Comment c SET c.replyCount = c.replyCount + 1 WHERE c.id = :commentId")
@Transactional
void incrementReplyCount(@Param("commentId") Long commentId);

/**
 * 原子操作：回复数-1
 */
@Modifying
@Query("UPDATE Comment c SET c.replyCount = c.replyCount - 1 WHERE c.id = :commentId AND c.replyCount > 0")
@Transactional
void decrementReplyCount(@Param("commentId") Long commentId);
```

---

## 📊 数据库迁移脚本

```sql
-- V4__add_comment_support.sql

-- 添加评论数字段
ALTER TABLE t_novel ADD COLUMN comment_count INT DEFAULT 0 COMMENT '评论数' AFTER favorite_count;

-- 添加评论数字段索引
CREATE INDEX idx_comment_count ON t_novel(comment_count);

-- 创建评论表
CREATE TABLE IF NOT EXISTS t_comment (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    novel_id BIGINT NOT NULL COMMENT '小说ID',
    parent_id BIGINT COMMENT '父评论ID（NULL表示顶层评论）',
    floor INT DEFAULT 1 COMMENT '楼层：1-顶层评论，2-回复评论',
    content TEXT NOT NULL COMMENT '评论内容',
    like_count INT DEFAULT 0 COMMENT '点赞数',
    reply_count INT DEFAULT 0 COMMENT '回复数（仅顶层评论）',
    deleted TINYINT DEFAULT 0 COMMENT '是否删除: 0-否, 1-是',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    
    INDEX idx_novel_id (novel_id),
    INDEX idx_parent_id (parent_id),
    INDEX idx_user_id (user_id),
    INDEX idx_created_at (created_at DESC),
    INDEX idx_novel_floor (novel_id, floor, created_at DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='评论表';
```

---

## 🧪 单元测试

### CommentServiceTest

```java
@Test
public void testAddTopLevelComment() {
    // 添加顶层评论
    Map<String, Object> result = commentService.addComment(1L, 1L, null, 1, "这本书太好看了！");
    assertTrue((Boolean) result.get("success"));
    
    // 验证评论数增加
    Novel novel = novelRepository.findById(1L).orElse(null);
    assertEquals(1, novel.getCommentCount());
}

@Test
public void testAddReply() {
    // 先添加顶层评论
    Comment topLevelComment = createTopLevelComment();
    
    // 添加回复
    Map<String, Object> result = commentService.addComment(2L, 1L, topLevelComment.getId(), 2, "同意！");
    assertTrue((Boolean) result.get("success"));
    
    // 验证评论数增加
    Novel novel = novelRepository.findById(1L).orElse(null);
    assertEquals(2, novel.getCommentCount());
    
    // 验证回复数增加
    Comment parentComment = commentRepository.findById(topLevelComment.getId()).orElse(null);
    assertEquals(1, parentComment.getReplyCount());
}

@Test
public void testDeleteComment() {
    // 添加评论
    Comment comment = createTopLevelComment();
    
    // 删除评论
    Map<String, Object> result = commentService.deleteComment(1L, comment.getId());
    assertTrue((Boolean) result.get("success"));
    
    // 验证评论数减少
    Novel novel = novelRepository.findById(1L).orElse(null);
    assertEquals(0, novel.getCommentCount());
}
```

---

## ⚠️ 注意事项

### 1. 权限控制

1. **未登录用户**：只能查看评论，不能评论、删除
2. **已登录用户**：可以评论、删除自己的评论
3. **管理员**：可以删除所有评论（待实现）

### 2. 内容审核

1. **敏感词过滤**：评论内容需要过滤敏感词（待实现）
2. **垃圾评论检测**：需要检测垃圾评论（待实现）
3. **人工审核**：可疑评论需要人工审核（待实现）

### 3. 并发问题

1. **评论数更新**：使用原子操作，避免并发冲突
2. **回复数更新**：使用原子操作，避免并发冲突

### 4. 性能优化

1. **分页加载**：顶层评论和回复都分页加载
2. **延迟加载**：回复列表默认折叠，点击展开
3. **缓存**：评论列表可以缓存（待实现）

---

## 📝 实施计划

| 阶段 | 任务 | 工作量 | 优先级 |
|------|------|--------|--------|
| 第一阶段 | 数据库迁移 | 0.5h | P0 |
| 第一阶段 | Comment实体开发 | 0.5h | P0 |
| 第一阶段 | CommentRepository开发 | 0.5h | P0 |
| 第一阶段 | CommentService开发 | 1h | P0 |
| 第一阶段 | CommentController开发 | 0.5h | P0 |
| 第二阶段 | 前端评论区域开发 | 2h | P0 |
| 第二阶段 | 前端添加/回复对话框开发 | 1h | P0 |
| 第二阶段 | 前端评论列表展示开发 | 1.5h | P0 |
| 第三阶段 | 后端单元测试 | 1h | P0 |
| 第三阶段 | 前后端联调 | 1h | P0 |
| 第三阶段 | 性能测试 | 0.5h | P1 |
| 第四阶段 | 点赞功能开发（可选） | 2h | P1 |
| 第四阶段 | 删除功能开发（可选） | 1h | P1 |

**总计**: 约12.5小时（核心功能9.5h，可选功能3h）

---

## ✅ 验收标准

### 功能验收

- [ ] 登录用户可以添加顶层评论
- [ ] 登录用户可以回复评论
- [ ] 未登录用户不能评论
- [ ] 评论数正确显示
- [ ] 回复数正确显示
- [ ] 评论列表正确展示
- [ ] 回复列表正确展示
- [ ] 用户可以删除自己的评论
- [ ] 评论数不会丢失更新（并发测试）

### 性能验收

- [ ] 获取评论列表 < 100ms
- [ ] 添加评论 < 200ms
- [ ] 删除评论 < 200ms

### 兼容性验收

- [ ] 未登录用户可以正常浏览（评论数正常显示）
- [ ] 登录用户评论功能正常
- [ ] 评论数并发更新正确

---

## 🎯 参考资料

### B站评论区特点

1. **顶层评论**：用户头像、昵称、时间、内容、点赞数、回复数
2. **回复列表**：用户头像、昵称、回复对象、时间、内容、点赞数
3. **回复折叠**：回复列表默认折叠，点击展开
4. **分页加载**：顶层评论和回复都分页加载
5. **点赞功能**：支持点赞/取消点赞

---

**文档结束**

请评审本设计文档，确认无误后我再进行开发！🦞
