# 权限验证统一和错误处理优化总结

**开发日期**: 2026-02-18
**开发人员**: OpenClaw
**版本**: v1.1
**状态**: ✅ 已完成

---

## 📋 任务完成情况

| 序号 | 任务 | 描述 | 状态 |
|------|------|------|------|
| 1 | 统一权限验证方式 | 使用Spring Security注解 | ✅ 已完成 |
| 2 | 记录到开发规范 | 创建开发规范文档 | ✅ 已完成 |
| 3 | 爬虫接口管理员权限 | ConfigController、CrawlerController等 | ✅ 已完成 |
| 4 | 统一错误处理 | 创建全局异常处理器 | ✅ 已完成 |
| 5 | 未登录自动跳转 | Axios拦截器 | ✅ 已完成 |

---

## 🗂️ 文件清单

### 新增文件（3个）

| 文件 | 说明 | 提交ID |
|------|------|--------|
| `开发规范.md` | 开发规范文档 | 2b184d0 |
| `GlobalExceptionHandler.java` | 全局异常处理器 | 2b184d0 |
| `ResourceNotFoundException.java` | 资源不存在异常 | 2b184d0 |

### 修改文件（5个）

| 文件 | 说明 | 提交ID |
|------|------|--------|
| `ConfigController.java` | 添加管理员权限验证 | 2b184d0 |
| `CrawlerController.java` | 添加管理员权限验证 | 2b184d0 |
| `TagsController.java` | 添加管理员权限验证 | 2b184d0 |
| `TaskController.java` | 添加管理员权限验证 | 2b184d0 |
| `api/index.js` | 添加Axios拦截器 | 2b184d0 |

---

## 📚 开发规范

### 1. 权限验证规范

#### 统一权限验证方式

**规则**: 所有接口的权限验证必须使用Spring Security注解，不要在Controller中手动验证。

---

#### 权限注解类型

| 注解 | 说明 | 示例 |
|------|------|------|
| `@PreAuthorize("isAuthenticated()")` | 需要登录 | 用户信息、收藏、评论 |
| `@PreAuthorize("hasRole('ADMIN')")` | 需要管理员 | 敏感词管理、爬虫配置 |
| `@PermitAll` | 公开接口 | 小说列表、标签查询 |

---

#### 示例代码

##### 1. 需要登录的接口

```java
@RestController
@RequestMapping("/api/favorites")
public class FavoriteController {

    @PreAuthorize("isAuthenticated()")
    @PostMapping
    public Map<String, Object> addFavorite(@RequestBody Map<String, Object> request) {
        Authentication authentication = SecurityContextHolder.getContext().getAuthentication();
        Long userId = (Long) authentication.getPrincipal();
        // 业务逻辑
    }
}
```

---

##### 2. 需要管理员的接口

```java
@RestController
@RequestMapping("/api/sensitive-words")
public class SensitiveWordController {

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping
    public Map<String, Object> getSensitiveWords(...) {
        // 业务逻辑
    }
}
```

---

### 2. 错误处理规范

#### 统一错误响应格式

```json
{
  "success": false,
  "message": "错误描述",
  "code": "ERROR_CODE"
}
```

---

#### 常见错误码

| 错误码 | 说明 | HTTP状态码 |
|--------|------|-----------|
| `UNAUTHORIZED` | 未登录 | 401 |
| `FORBIDDEN` | 权限不足 | 403 |
| `NOT_FOUND` | 资源不存在 | 404 |
| `BAD_REQUEST` | 请求参数错误 | 400 |
| `INTERNAL_ERROR` | 服务器内部错误 | 500 |

---

### 3. 前端开发规范

#### 未登录自动跳转

**规则**: 所有需要登录的接口，如果返回401（UNAUTHORIZED）或"未登录"错误，前端必须自动跳转到登录界面。

---

#### 实现方式

##### Axios拦截器

```javascript
// 请求拦截器：添加Token
api.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

// 响应拦截器：处理错误
api.interceptors.response.use(
  response => response.data,
  error => {
    // 处理401错误（未登录）
    if (error.response && error.response.status === 401) {
      ElMessage.error('请先登录')
      router.push('/login')
      return Promise.reject(error)
    }

    // 处理403错误（权限不足）
    if (error.response && error.response.status === 403) {
      ElMessage.error('权限不足，仅管理员可访问')
      return Promise.reject(error)
    }

    // 处理其他错误
    const message = error.response?.data?.message || error.message || '请求失败'
    ElMessage.error(message)
    return Promise.reject(error)
  }
)
```

---

## 🔧 后端实现

### 1. 全局异常处理器

```java
/**
 * 全局异常处理器
 */
@Slf4j
@RestControllerAdvice
public class GlobalExceptionHandler {

    /**
     * 处理权限不足异常
     */
    @ExceptionHandler(AccessDeniedException.class)
    public ResponseEntity<Map<String, Object>> handleAccessDeniedException(AccessDeniedException e) {
        log.warn("权限不足: {}", e.getMessage());
        Map<String, Object> result = new HashMap<>();
        result.put("success", false);
        result.put("message", "权限不足，仅管理员可访问");
        result.put("code", "FORBIDDEN");
        return ResponseEntity.status(HttpStatus.FORBIDDEN).body(result);
    }

    /**
     * 处理认证失败异常
     */
    @ExceptionHandler(AuthenticationException.class)
    public ResponseEntity<Map<String, Object>> handleAuthenticationException(AuthenticationException e) {
        log.warn("未登录: {}", e.getMessage());
        Map<String, Object> result = new HashMap<>();
        result.put("success", false);
        result.put("message", "请先登录");
        result.put("code", "UNAUTHORIZED");
        return ResponseEntity.status(HttpStatus.UNAUTHORIZED).body(result);
    }

    /**
     * 处理参数错误异常
     */
    @ExceptionHandler(IllegalArgumentException.class)
    public ResponseEntity<Map<String, Object>> handleIllegalArgumentException(IllegalArgumentException e) {
        log.warn("参数错误: {}", e.getMessage());
        Map<String, Object> result = new HashMap<>();
        result.put("success", false);
        result.put("message", e.getMessage());
        result.put("code", "BAD_REQUEST");
        return ResponseEntity.status(HttpStatus.BAD_REQUEST).body(result);
    }

    /**
     * 处理资源不存在异常
     */
    @ExceptionHandler(ResourceNotFoundException.class)
    public ResponseEntity<Map<String, Object>> handleResourceNotFoundException(ResourceNotFoundException e) {
        log.warn("资源不存在: {}", e.getMessage());
        Map<String, Object> result = new HashMap<>();
        result.put("success", false);
        result.put("message", e.getMessage());
        result.put("code", "NOT_FOUND");
        return ResponseEntity.status(HttpStatus.NOT_FOUND).body(result);
    }

    /**
     * 处理其他异常
     */
    @ExceptionHandler(Exception.class)
    public ResponseEntity<Map<String, Object>> handleException(Exception e) {
        log.error("服务器内部错误: {}", e.getMessage(), e);
        Map<String, Object> result = new HashMap<>();
        result.put("success", false);
        result.put("message", "服务器内部错误");
        result.put("code", "INTERNAL_ERROR");
        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(result);
    }
}
```

---

### 2. 自定义异常类

```java
/**
 * 资源不存在异常
 */
public class ResourceNotFoundException extends RuntimeException {
    public ResourceNotFoundException(String message) {
        super(message);
    }
}
```

---

### 3. 管理员权限验证

#### ConfigController

```java
/**
 * 爬虫配置控制器（仅管理员可访问）
 */
@Slf4j
@RestController
@RequestMapping("/api/crawler/configs")
public class ConfigController {

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping
    public ResponseEntity<Iterable<CrawlerConfig>> getAllConfigs() {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping("/{id}")
    public ResponseEntity<CrawlerConfig> getConfigById(@PathVariable Long id) {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @PutMapping("/{id}")
    public ResponseEntity<Map<String, Object>> updateConfig(@PathVariable Long id, @RequestBody Map<String, Object> updates) {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping("/platform/{platform}")
    public ResponseEntity<CrawlerConfig> getConfigByPlatform(@PathVariable String platform) {
        // 业务逻辑
    }
}
```

---

#### CrawlerController

```java
/**
 * 爬虫控制器（仅管理员可访问）
 */
@Slf4j
@RestController
@RequestMapping("/api/crawler")
public class CrawlerController {

    @PreAuthorize("hasRole('ADMIN')")
    @PostMapping("/trigger")
    public Map<String, Object> triggerCrawler() {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @PostMapping("/trigger/{platform}")
    public Map<String, Object> triggerCrawlerByPlatform(@PathVariable String platform) {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping("/test/{platform}")
    public ResponseEntity<Map<String, Object>> testCrawler(@PathVariable String platform,
                                                           @RequestParam(required = false) String tag) {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping("/status/{platform}")
    public Map<String, Object> getCrawlerStatus(@PathVariable String platform) {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping("/novels")
    public List<Novel> getAllNovels() {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping("/novels/page")
    public ResponseEntity<Map<String, Object>> getNovelsPage(
            @RequestParam(defaultValue = "0") int page,
            @RequestParam(defaultValue = "10") int size,
            @RequestParam(required = false) String platform,
            @RequestParam(required = false) String tag) {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping("/novels/platform/{platform}")
    public List<Novel> getNovelsByPlatform(@PathVariable String platform) {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping("/configs")
    public Object getAllConfigs() {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping("/crawlers")
    public Map<String, String> getAllCrawlers() {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping("/health")
    public Map<String, Object> health() {
        // 业务逻辑
    }
}
```

---

#### TagsController

```java
/**
 * 标签Controller（仅管理员可访问）
 */
@Slf4j
@RestController
@RequestMapping("/api/crawler/novels/tags")
public class TagsController {

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping
    public ResponseEntity<Map<String, Object>> getAllTags() {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping("/platform/{platform}")
    public ResponseEntity<Map<String, Object>> getTagsByPlatform(@PathVariable String platform) {
        // 业务逻辑
    }
}
```

---

#### TaskController

```java
/**
 * 爬虫任务控制器（仅管理员可访问）
 */
@Slf4j
@RestController
@RequestMapping("/api/crawler/tasks")
public class TaskController {

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping
    public ResponseEntity<Map<String, Object>> getTasks(
            @RequestParam(defaultValue = "0") int page,
            @RequestParam(defaultValue = "10") int size,
            @RequestParam(required = false) String platform,
            @RequestParam(required = false) String status) {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @GetMapping("/{id}")
    public ResponseEntity<CrawlerTask> getTask(@PathVariable Long id) {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @PostMapping
    public ResponseEntity<CrawlerTask> createTask(@RequestBody CrawlerTask task) {
        // 业务逻辑
    }

    @PreAuthorize("hasRole('ADMIN')")
    @PutMapping("/{id}")
    public ResponseEntity<CrawlerTask> updateTask(@PathVariable Long id, @RequestBody CrawlerTask task) {
        // 业务逻辑
    }
}
```

---

## 🎨 前端实现

### 1. Axios拦截器

```javascript
import axios from 'axios'
import { ElMessage } from 'element-plus'
import router from '../router'
import mockApi from '../mock'

const api = axios.create({
  baseURL: '/api',
  timeout: 30000
})

// 请求拦截器：添加Token
api.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

// 响应拦截器：处理错误
api.interceptors.response.use(
  response => response.data,
  error => {
    // 处理401错误（未登录）
    if (error.response && error.response.status === 401) {
      ElMessage.error('请先登录')
      router.push('/login')
      return Promise.reject(error)
    }

    // 处理403错误（权限不足）
    if (error.response && error.response.status === 403) {
      ElMessage.error('权限不足，仅管理员可访问')
      return Promise.reject(error)
    }

    // 处理其他错误
    const message = error.response?.data?.message || error.message || '请求失败'
    ElMessage.error(message)
    return Promise.reject(error)
  }
)
```

---

## 📊 修改统计

| 类型 | 数量 |
|------|------|
| 新增文件 | 3 |
| 修改文件 | 5 |
| 添加权限验证的Controller | 4 |
| 添加权限验证的接口 | 20 |
| 添加异常处理器 | 5 |

---

## 🎯 改进效果

### 1. 权限验证方式统一

**改进前**：
- 有些使用Authentication参数手动验证
- 有些使用SecurityContextHolder手动验证
- 有些使用自定义验证方法
- 有些没有验证

**改进后**：
- 统一使用Spring Security注解
- 代码简洁明了
- 减少代码冗余

---

### 2. 错误处理统一

**改进前**：
- 每个接口需要手动处理错误
- 错误消息不统一
- 错误码不统一

**改进后**：
- 使用全局异常处理器
- 统一错误响应格式
- 统一错误码

---

### 3. 未登录自动跳转

**改进前**：
- 未登录需要手动跳转
- 用户体验差

**改进后**：
- 自动跳转到登录界面
- 用户体验好

---

### 4. 代码可维护性提高

**改进前**：
- 代码冗余
- 难以维护

**改进后**：
- 代码简洁
- 易于维护

---

## 🎯 Git提交记录

| 提交ID | 提交信息 | 时间 |
|--------|---------|------|
| 2b184d0 | refactor: 统一权限验证方式和错误处理 | 18:00 |

### GitHub推送记录

- **仓库**: https://github.com/lridea/novel-reader
- **分支**: master
- **最新提交**: 2b184d0
- **提交总数**: 1个

---

## ✅ 验收标准

### 功能验收

- [x] 统一权限验证方式（使用Spring Security注解）
- [x] 创建开发规范文档
- [x] 爬虫接口添加管理员权限验证
- [x] 统一错误处理（全局异常处理器）
- [x] 未登录自动跳转到登录界面

---

### 代码验收

- [x] 移除手动权限验证代码
- [x] 移除SecurityContextHolder手动验证
- [x] 移除自定义验证方法
- [x] 统一使用@PreAuthorize注解
- [x] 统一错误响应格式

---

## 🎓 技术亮点

### 1. 全局异常处理器

**特点**：
- 统一错误处理
- 统一错误响应格式
- 减少代码冗余

---

### 2. 权限验证统一

**特点**：
- 使用Spring Security注解
- 代码简洁
- 易于维护

---

### 3. Axios拦截器

**特点**：
- 自动添加Token
- 自动处理401错误
- 自动跳转到登录界面

---

## 📝 待办事项

### 功能待优化

- [ ] 添加更多自定义异常类
- [ ] 添加API文档
- [ ] 添加单元测试

---

## 🏆 总结

### 完成情况

✅ **核心功能已全部完成**：
- 统一权限验证方式
- 创建开发规范文档
- 爬虫接口添加管理员权限验证
- 统一错误处理
- 未登录自动跳转

---

### 技术亮点

1. ✅ **全局异常处理器**
2. ✅ **权限验证统一**
3. ✅ **Axios拦截器**
4. ✅ **开发规范文档**

---

### 后续优化

1. ⚠️ **功能扩展**：添加更多自定义异常类
2. ⚠️ **文档完善**：添加API文档
3. ⚠️ **测试完善**：添加单元测试

---

**开发完成时间**: 2026-02-18 18:00
**开发人员**: OpenClaw
**GitHub仓库**: https://github.com/lridea/novel-reader
**最新提交**: 2b184d0
**开发状态**: ✅ 权限验证统一和错误处理优化已完成

🦞
